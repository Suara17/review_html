# Python 卡片组整理方案

更新时间：2026-06-06

## 整理目标
把当前 `topics-data/python.json` 的 57 条内容，压缩成一套更适合面试背诵的高频卡片：
- 只保留高频核心题
- 去掉代码示例
- 每张卡片控制为“1句结论 + 2~5条短点”
- 尽量把重复题合并

---

## 当前原始条目数
- 原始条目：**57 条**
- 目标精简后：**约 28~32 条**

---

## 建议保留的高频核心卡片

### A. 基础语义与对象模型
1. `==` 和 `is` 的区别
2. Python 基本数据类型
3. `list` 和 `tuple` 的区别
4. 浅拷贝和深拷贝
5. 函数参数传递：可变对象 vs 不可变对象
6. 可变对象作为默认参数的陷阱
7. 类变量和实例变量的区别
8. `__new__` 和 `__init__` 的区别
9. 单下划线、双下划线的含义
10. Python 中的作用域规则（LEGB）

### B. 函数与语法机制
11. `*args` 和 `**kwargs`
12. 闭包 与 `nonlocal`
13. 装饰器
14. 装饰器链
15. `lambda`、`map`、`filter`、`reduce`
16. `and`、`or`、`not` 的返回规律
17. `if __name__ == "__main__"` 的作用
18. 字符串格式化：`%`、`.format()`、`f-string`

### C. 面向对象高频题
19. 实例方法、类方法、静态方法的区别
20. `@property` 的作用
21. MRO 与 `super()`
22. 魔术方法（只保留概念与常见用途）
23. `__slots__` 的作用
24. 鸭子类型

### D. 迭代、生成与异步并发
25. 迭代器和生成器
26. 生成器的 `send()`
27. 协程 与 `async/await`
28. 多线程 vs 多进程 vs 协程
29. GIL 的本质与影响

### E. 运行时与工程化
30. 垃圾回收机制（GC）
31. 上下文管理器与 `with`
32. Python 语言特点 与 解释型/编译型区别
33. Python 解释器种类及特点
34. 自省（introspection）

---

## 建议合并的条目
以下内容存在明显重叠，建议合并整理：

1. `@staticmethod 与 @classmethod 的区别`
   + `实例方法、类方法、静态方法的详细对比`
   -> 合并为 **“实例方法、类方法、静态方法的区别”**

2. `上下文管理器与with语句`
   + `上下文管理器协议与自定义计时器`
   -> 合并为 **“上下文管理器与 with”**

3. `闭包`
   + `global 和 nonlocal 关键字`
   -> 以 **“作用域、闭包与 nonlocal”** 为主线整理

4. `lambda 函数`
   + `map、filter、reduce`
   -> 合并为 **“函数式编程基础”**

5. `协程（Coroutine）与 async/await`
   + `多线程 vs 多进程 vs 协程`
   + `GIL`
   -> 作为一个并发模块分开三张卡，但相互呼应

---

## 建议降级或删除的低频条目
这些可以不进主背诵版，最多放到附录：

1. 常用 Python 库有哪些
2. 列表和字典的常用方法核心要点
3. 自定义类的深拷贝行为
4. 字典推导式
5. Python 中为什么没有函数重载
6. 新式类和旧式类（经典类）的区别
7. `read()` / `readline()` / `readlines()`
8. Python 2 和 Python 3 的主要区别
9. 元类（metaclass）简介
10. 虚拟环境（Virtual Environment）
11. 递归
12. 类装饰器 vs 函数装饰器
13. 元编程（Metaprogramming）
14. PEP8 / Python 之禅
15. docstring 与类型注解
16. 命名规范与注释规范
17. 代码风格工具
18. Python 单例模式（如果是后端岗位可保留；若强调高频背诵，可放附录）

---

## 建议的最终背诵顺序

### 第一部分：基础必背
1. `==` 和 `is`
2. 基本数据类型
3. list vs tuple
4. 浅拷贝 vs 深拷贝
5. 可变对象默认参数陷阱
6. 参数传递：可变 vs 不可变
7. LEGB 作用域
8. 类变量 vs 实例变量
9. `__new__` vs `__init__`
10. 下划线命名含义

### 第二部分：函数与高级语法
11. `*args` / `**kwargs`
12. 闭包与 `nonlocal`
13. 装饰器
14. 装饰器链
15. `lambda` + `map/filter/reduce`
16. `and/or/not` 返回值规律
17. `__main__`
18. 字符串格式化

### 第三部分：面向对象
19. 实例方法 / 类方法 / 静态方法
20. `@property`
21. MRO / `super()`
22. 魔术方法
23. `__slots__`
24. 鸭子类型

### 第四部分：迭代与并发
25. 迭代器 vs 生成器
26. `send()`
27. 协程与 `async/await`
28. GIL
29. 多线程 / 多进程 / 协程

### 第五部分：运行时与工程
30. 垃圾回收
31. 上下文管理器与 `with`
32. Python 语言特点
33. Python 解释器
34. 自省

---

## 下一步执行
下一步我会开始输出 **Python 精简背诵版初稿**，按上面的顺序一张一张改写，再回写到 JSON。
